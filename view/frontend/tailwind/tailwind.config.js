/*
 * Tailwind v3 (Hyvä 2) content globs, merged by mergeTailwindConfig().
 *
 * Best-effort back-compat only — Hyvä 3 ignores this in favour of module.css
 * and its @source directive.
 */
module.exports = {
    content: [
        '../templates/**/*.phtml',
    ],
};
